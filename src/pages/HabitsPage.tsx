import { HabitsSection } from "@/components/habits/HabitsSection";

export function HabitsPage() {
	// const { habits } = useHabits({ type: "list", queries: {} });
	return (
		<>
			<HabitsSection />
		</>
	);
}
